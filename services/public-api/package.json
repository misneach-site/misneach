{
  "name": "public-api",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "type": "commonjs",
  "scripts": {
    "prebuild": "npm run build --workspace @misneach/public-flows",
    "build": "tsc -p tsconfig.json",
    "premigrate:mariadb": "npm run build --workspace @misneach/public-flows",
    "migrate:mariadb": "node -r ts-node/register src/migrations/mariadb-to-dynamodb.ts",
    "premigrate:mariadb:write": "npm run build --workspace @misneach/public-flows",
    "migrate:mariadb:write": "node -r ts-node/register src/migrations/mariadb-to-dynamodb.ts --write",
    "prestart:local": "npm run build --workspace @misneach/public-flows",
    "start:local": "node -r ts-node/register src/local/start.ts",
    "pretest": "npm run build --workspace @misneach/public-flows",
    "test": "jest --config jest.config.cjs",
    "pretest:integration": "npm run build --workspace @misneach/public-flows",
    "test:integration": "RUN_FLOCI_INTEGRATION=1 jest --config jest.config.cjs --runTestsByPath src/local/public-api.floci.integration.spec.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.758.0",
    "@aws-sdk/client-sqs": "^3.1106.0",
    "@aws-sdk/client-ssm": "^3.1106.0",
    "@aws-sdk/lib-dynamodb": "^3.758.0",
    "@misneach/public-flows": "file:../../packages/public-flows",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
